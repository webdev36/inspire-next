<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? content_for(:title) : "Live Inspired" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= yield :head_js %>
  </head>
  <body>

    <div id="top-menu" class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <% if user_signed_in? %>
            <%= link_to "Live Inspired", user_path(current_user), class:"brand" %>
          <% else %>
            <%= link_to "Live Inspired", root_path, class:"brand" %>
          <% end %>
          <div class="container nav-collapse">
            <ul class="nav">
              <% if user_signed_in? %>
                <li><%= link_to 'Channels', channels_path%> </li>
                <li><%= link_to 'Subscribers', subscribers_path%> </li>
                <li><%= link_to 'Profile', edit_user_registration_path(current_user)%></li>
                <li><%= link_to "Sign out", destroy_user_session_path, method: :delete  %></li>
              <% else %>
                <li><%= link_to "Sign in", new_user_session_path  %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
          <div id="page" class="span12">
            <%= bootstrap_flash %>
            <%= content_for?(:content) ? yield(:content) : yield %>
          </div>
        </div><!--/row-->

      <footer class="voffset20">
        <p>&copy; ILiveInspired 2013-2015. Portions &copy; OMAlab, Inc. All rights reserved.</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
